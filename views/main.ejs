
<form action="/recherche" method="post" id="recherche">
    <input type="text" name="prenom" id="zone"><br>
    <input type="button" id="rechercher" value="Rechercher">
</form>


<form id="mon_formulaire" action="/modifier" method="POST">
    <input type="text" name="_id">
    <input type="text" name="prenom">
    <input type="text" name="nom">
    <input type="text" name="telephone">
    <input type="text" name="email">
</form> 

<table class="tableau">
        <% let noligne = 0 %>
            <% for(elm of adresses) { %>
                <% if (noligne==0) { %>
                    <tr>
                        <% noligne++ %>
                        <td><a href="/trier/_id/<%= ordre %>">id</a></td>
                        <td><a href="/trier/prenom/<%= ordre %>">prenom</a></td>
                        <td><a href="/trier/nom/<%= ordre %>">nom</a></td>
                        <td><a href="/trier/telephone/<%= ordre %>">telephone</a></td>
                        <td><a href="/trier/email/<%= ordre %>">email</a></td>
                        <td><a href="http://localhost:8081/formulaire">Ajouter</a></td>
                    </tr>
                <% } %>
    
                
    
                    <tr>
                        <% for(info in elm) { %>
                            <td class="<%= info %>" contenteditable="true"><%= elm[info] %></td>
                        <% } %>
                        <td><a href="/detruire/<%= elm._id %>">Détruire </a></td>
                        <td><a href="" class="modifier" >Sauver </a></td>
                        <td><a href="/profil/<%= elm._id %>">Profil</a></td>
                    </tr>            
            <% } %>
    </table>

